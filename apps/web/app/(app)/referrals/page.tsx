import { PlaceholderPage } from "../_components/app-shell";

export default function ReferralsPage() {
  return (
    <PlaceholderPage
      title="Referrals"
      body="Referral tracking will use the same dark product shell and billing-aware account context."
    />
  );
}
