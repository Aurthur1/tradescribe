import { PlaceholderPage } from "../_components/app-shell";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      body="A full-page P&L calendar and activity heatmap will expand the dashboard calendar into a review-first trading history view."
    />
  );
}
