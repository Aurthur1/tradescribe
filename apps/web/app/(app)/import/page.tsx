import { PlaceholderPage } from "../_components/app-shell";

export default function ImportPage() {
  return (
    <PlaceholderPage
      title="Import"
      body="Read-only MetaTrader connection management and future CSV import flows will start from this screen."
    />
  );
}
