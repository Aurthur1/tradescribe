import { PlaceholderPage } from "../_components/app-shell";

export default function ContactPage() {
  return (
    <PlaceholderPage
      title="Contact"
      body="Support and sales contact options will appear here with account context and clear advisory-only framing."
    />
  );
}
